# Backend dependencies — AI Resume Analyzer
# Versões fixadas para garantir reprodutibilidade

# Web framework
fastapi==0.111.0

# Servidor ASGI
uvicorn[standard]==0.29.0

# Suporte a multipart/form-data (upload de arquivos)
python-multipart==0.0.9

# Extração de texto de PDF
pymupdf==1.24.3

# Validação de dados e schemas
pydantic==2.7.1

# Persistência — ORM, migrations e driver PostgreSQL (SPEC 0004)
sqlalchemy==2.0.51
alembic==1.13.3
psycopg2-binary==2.9.12

# Cliente HTTP — usado em runtime por app/services/llm_providers/ollama_provider.py (SPEC 0014)
httpx==0.27.0
