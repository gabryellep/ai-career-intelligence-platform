# ============================================================
# frontend/.env.example — AI Resume Analyzer
# Copie para frontend/.env quando rodar o frontend fora do Docker
# e precisar apontar para um backend diferente do proxy padrão do Vite.
# ============================================================

# URL do backend usada pelo browser. Em desenvolvimento local via
# docker compose, aponta para o backend publicado em localhost:8000.
VITE_API_BASE_URL=http://localhost:8000
